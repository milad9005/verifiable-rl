# verifiable-rl

Reinforcement learning with verifiable rewards — from-scratch implementations
through GRPO training and verifier design.

## Structure

- `foundations/` — autograd and policy gradient methods implemented from scratch
- `grpo/` — GRPO training runs on verifiable tasks
- `verifiers/` — deterministic reward verifiers
- `results/` — measurements and plots
